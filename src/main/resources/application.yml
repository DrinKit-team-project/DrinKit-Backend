spring:
  profiles:
    active: develop # 기본 profile 선택

# develop 환경
---
spring:
  profiles: develop
  jpa:
    database: mysql
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
    properties:
            hibernate:
              format_sql: true

  datasource:
          username: jiwon
          password: jiwon
          url: jdbc:mysql://localhost/drinkit
          driverClassName: com.mysql.jdbc.Driver
  security:
    basic:
      enabled: false # security 기본 인증 옵션 제거

# 로그 레벨 설정
logging:
  pattern:
    level: trace

jwt:
  signingKey: drinkitjwtsecretkey

skipUrls: /social
noToSkipPath: /api/**

# 운영환경
---
spring.profiles: real
spring.profiles.include: real-db
spring:
  datasource:
      url: jdbc:mysql://drinkit.c114gl6uea4l.ap-northeast-2.rds.amazonaws.com:3306/drinkit
      username: jiwon
      password: drinkitjiwon
      driverClassName: com.mysql.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
            hibernate:
              format_sql: true

# 로그 레벨 설정
logging:
  pattern:
    level: info

---